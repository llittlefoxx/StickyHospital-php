{% extends "BackendMedecinBundle::layout.html.twig" %}
{#{% extends '::base.html.twig' %}#}
{% block haed %}
    {{ parent() }}
{% block title %}{{ parent() }} Home Page {% endblock %}
{% block stylesheet %}
    {{ parent() }}
  <style type="text/css">
      /* FROM HTTP://WWW.GETBOOTSTRAP.COM
        * Glyphicons
        *
        * Special styles for displaying the icons and their classes in the docs.
        */

      .bs-glyphicons {
        padding-left: 0;
        padding-bottom: 1px;
        margin-bottom: 20px;
        list-style: none;
        overflow: hidden;
      }
      .bs-glyphicons li {
        float: left;
        width: 25%;
        height: 115px;
        padding: 10px;
        margin: 0 -1px -1px 0;
        font-size: 12px;
        line-height: 1.4;
        text-align: center;
        border: 1px solid #ddd;
      }
      .bs-glyphicons .glyphicon {
        margin-top: 5px;
        margin-bottom: 10px;
        font-size: 24px;
      }
      .bs-glyphicons .glyphicon-class {
        display: block;
        text-align: center;
        word-wrap: break-word; /* Help out IE10+ with class names */
      }
      .bs-glyphicons li:hover {
        background-color: rgba(86,61,124,.1);
      }

      @media (min-width: 768px) {
        .bs-glyphicons li {
          width: 12.5%;
        }
      }
      li{
          list-style: outside none none;
      }
    </style>
    {% endblock %}
    {% endblock %}
{% block body -%}
        {% block menu %}
    <!-- Left side column. contains the logo and sidebar -->
    <aside class="main-sidebar">

        <!-- sidebar: style can be found in sidebar.less -->
        <section class="sidebar">

            <!-- Sidebar user panel (optional) -->
            <div class="user-panel">
                <div class="pull-left image">
                    <img src="dist/img/user2-160x160.jpg" class="img-circle" alt="User Image" />
                </div>
                <div class="pull-left info">
                    <p>Alexander Pierce</p>
                    <!-- Status -->
                    <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
                </div>
            </div>

            <!-- Sidebar Menu -->
            <ul class="sidebar-menu">
                <!-- Optionally, you can add icons to the links -->
                <li><a href="#">Planification des RDVs</a><</li>
               <li class="treeview active">
                    <a href="{{ path('backend_hospital_affichedossiermedical') }}">
                        <i class="fa fa-folder"></i> <span>Dossier Médical</span><i class="fa fa-angle-left pull-right"></i>
                    </a>
                        <ul class="treeview-menu menu-open" style="display: block;">
                            <li><a href="">Show</a></li>
                            
                        </ul>
                </li>

            </ul><!-- /.sidebar-menu -->
        </section>
        <!-- /.sidebar -->
    </aside>
{% endblock %}
      {% block content %}
     <section class="content-header">
                <h1>
                    List des dossiers Médical
                </h1>
                <ol class="breadcrumb">
                    <li><a href="#"><i class="fa fa-dashboard"></i>Dashboard</a></li>
                    <li class="active">Dossier Médical</li>
                </ol>
            </section>
    {%endblock%}
    {% block main %}
        {% block box %}
        
    <div class="box-body">
       <table id="example1" class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Nom Patient</th>
                <th>Numéro du Dossier</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.idDossierpatient.idPatient.nomPatient }}</td>
                <td><a href="{{ path('dossiermedical_show', { 'id': entity.idDossierMedical }) }}">{{ entity.idDossierMedical }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('dossiermedical_show', { 'id': entity.idDossierMedical }) }}"><i class="fa fa-fw fa-eye"></i>Show</a>
                    </li>
                    <li>
                        <a href="{{ path('dossiermedical_edit', { 'id': entity.idDossierMedical }) }}"><i class="fa fa-edit"></i>Edit</a>
                    </li>
                </ul>
                </td>
            </tr>
            {%endfor%}
       
        </tbody>
    </table>
</div>
      {#  <ul>
        <li>
            <a href="{{ path('dossiermedical_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>#}
            </div>
        </div>
{% endblock %}
  {% endblock %}
{% endblock %}