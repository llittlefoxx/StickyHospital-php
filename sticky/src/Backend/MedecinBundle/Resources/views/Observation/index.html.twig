{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Observation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texte</th>
                <th>Dateobservation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('observation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.texte }}</td>
                <td>{% if entity.dateObservation %}{{ entity.dateObservation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('observation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('observation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('observation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
