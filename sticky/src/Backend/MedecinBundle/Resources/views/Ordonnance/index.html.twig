{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ordonnance list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dateordonnance</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ordonnance_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateOrdonnance %}{{ entity.dateOrdonnance|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ordonnance_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ordonnance_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ordonnance_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
