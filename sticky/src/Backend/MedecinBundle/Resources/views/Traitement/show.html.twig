{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Traitement</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libtraitement</th>
                <td>{{ entity.libTraitement }}</td>
            </tr>
            <tr>
                <th>Prixtraitement</th>
                <td>{{ entity.prixTraitement }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('traitement') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('traitement_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
